"""Structural SVG gating: provenance and quality classifiers.

`features` exposes the autotrace-detector feature extractor; `autotrace`
turns a feature vector into a keep/reject verdict for the corpus gate.
"""
